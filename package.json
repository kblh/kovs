{
  "scripts": {
    "start": "eleventy --serve & npx @tailwindcss/cli -i styles/tailwind.css -o _site/style.css --watch",
    "build": "ELEVENTY_PRODUCTION=true eleventy && NODE_ENV=production npx @tailwindcss/cli -i styles/tailwind.css -o _site/style.css --minify"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@tailwindcss/cli": "^4.1.18",
    "alpinejs": "^3.7.0",
    "html-minifier": "^4.0.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^4.1.0"
  }
}
